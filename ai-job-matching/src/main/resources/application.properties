spring.datasource.url=${DB_URL:jdbc:postgresql://dpg-d6mn5s6a2pns73d9thmg-a:5432/ai_job_matcher}
spring.datasource.username=${DB_USERNAME:ai_user}
spring.datasource.password=${DB_PASSWORD:ORxQVnoczp5Ynplg5FldRaI4uiNVNxp9}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.hikari.maximum-pool-size=${DB_MAX_POOL_SIZE:5}
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:update}
spring.jpa.show-sql=${JPA_SHOW_SQL:false}
spring.jpa.open-in-view=false

server.port=${PORT:${SERVER_PORT:8080}}

app.mail.enabled=${MAIL_ENABLED:true}
app.mail.from=${MAIL_FROM:}
app.mail.provider=${MAIL_PROVIDER:brevo}
app.mail.brevo.api-key=${BREVO_API_KEY:}
app.mail.brevo.base-url=${BREVO_BASE_URL:https://api.brevo.com/v3/smtp/email}
app.mail.connection-timeout-ms=${MAIL_CONNECTION_TIMEOUT_MS:5000}
app.mail.read-timeout-ms=${MAIL_READ_TIMEOUT_MS:10000}

adzuna.app_id=${ADZUNA_APP_ID:}
adzuna.app_key=${ADZUNA_APP_KEY:}
adzuna.country=${ADZUNA_COUNTRY:in}

jobs.import.scheduler.enabled=${JOBS_IMPORT_SCHEDULER_ENABLED:false}
jobs.import.scheduler.fixed-rate-ms=${JOBS_IMPORT_SCHEDULER_FIXED_RATE_MS:3600000}
jobs.import.scheduler.initial-delay-ms=${JOBS_IMPORT_SCHEDULER_INITIAL_DELAY_MS:60000}
